// The OAuth step, on the web because it cannot happen inside WhatsApp.
//
// Growth and Business sellers link their own Facebook Page and Instagram
// Business account here, once. Phase 5 — the Meta app review has to clear
// before instagram_content_publish will work for anyone.
export default function ConnectChannels() {
  return (
    <div className="flex min-h-dvh items-center justify-center bg-bg px-4">
      <div className="card w-full max-w-sm p-6 text-center text-sm text-muted">
        Channel connection is not built yet.
      </div>
    </div>
  );
}
